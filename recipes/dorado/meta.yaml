{% set name = "dorado" %}
{% set version = "1.3.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://cdn.oxfordnanoportal.com/software/dorado/dorado-{{ version }}-linux-x64.tar.gz
  sha256: 92892733f411e78193e61a33e882347474b691f52753496488b6922c98d19f10

build:
  number: 0
  skip: true  # [not linux]
  script: |
    mkdir -p $PREFIX/bin
    cp dorado-{{ version }}-linux-x64/bin/dorado $PREFIX/bin/
    chmod +x $PREFIX/bin/dorado
  run_exports:
    - {{ pin_subpackage(name, max_pin="x") }}

requirements:
  run:
    - libgcc-ng
    - libstdcxx-ng

test:
  commands:
    - dorado --version

about:
  home: https://github.com/nanoporetech/dorado
  license: ONT Public License
  license_family: Other
  license_file: LICENSE
  summary: 'Dorado: Oxford Nanopore Technologies basecaller'
  description: |
    Dorado is a high-performance, easy-to-use, open source basecaller 
    for Oxford Nanopore sequencing data.
  dev_url: https://github.com/nanoporetech/dorado
  doc_url: https://github.com/nanoporetech/dorado

extra:
  identifiers:
    - doi:10.1038/s41587-023-01643-w
  additional-platforms:
    - linux-aarch64
    - osx-arm64
  skip-lints:
    - should_be_noarch_generic
  recipe-maintainers:
    - SiYangming